services:
  - type: web
    name: face-backend
    env: python
    rootDir: face-backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 2 --timeout 180
    autoDeploy: true
